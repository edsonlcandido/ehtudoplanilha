# Variáveis de ambiente para Google Sheets API (acesso a planilhas)
# Nota: OAuth para autenticação (login/registro) é configurado diretamente no PocketBase Admin UI
GOOGLE_REDIRECT_URI=http://localhost:8090/google-oauth-callback